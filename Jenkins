#!groovy

def checkoutCode() {
    stage 'checkout'
    checkout scm "https://github.com/codesqueak/Z80Processor.git"
}

def build() {
    stage 'build'
    sh './gradlew clean build -x test'
}


stage 'execute build'

node {
  checkoutCode()
  build()
}
